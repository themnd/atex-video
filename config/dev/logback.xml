<configuration scan="true" scanPeriod="30 seconds" debug="true">


  <!-- CONSOLE APPENDER -->
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">

    <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
      <pattern>%d{HH:mm:ss.SSS} [%thread] [S: %X{session} U: %X{username}] %-5level %logger - %msg%n</pattern>
    </encoder>
  </appender>

  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <file>jetty.log</file>
    <append>false</append>
    <encoder>
      <Pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger - %msg%n</Pattern>
    </encoder>
  </appender>


  <!-- LOGGERS -->
  <logger name="com.atex.milan" level="TRACE" />

  <logger name="org.springframework" level="WARN" />

  <logger name="org.apache.cxf" level="WARN" />

  <logger name="org.apache.camel" level="WARN" />

  <logger name="org.jboss" level="WARN" />

  <logger name="org.apache.http" level="WARN" />

  <logger name="org.eclipse.jetty" level="ERROR" />

  <logger name="com.atomikos" level="WARN" />

  <!-- global -->
  <logger name="global" level="INFO" />

  <!-- tomcat -->
  <logger name="org.apache.catalina" level="INFO" />
  <logger name="org.apache.coyote" level="INFO" />
  <logger name="org.apache.jasper" level="INFO" />
  <logger name="org.apache.tomcat" level="INFO" />

  <!-- jetty -->
  <logger name="org.mortbay" level="INFO" />
  <logger name="org.eclipse.jetty" level="INFO" />
  <logger name="Sisu" level="INFO" />

  <!-- ROOT LOGGER -->
  <root level="INFO">
    <appender-ref ref="FILE" />
    <appender-ref ref="STDOUT" />
  </root>

  <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
    <!-- reset all previous level configurations of all j.u.l. loggers -->
    <resetJUL>true</resetJUL>
  </contextListener>

  <!-- LevelChangePropagator propagates changes made to the level of any logback-classic logger onto the java.util.logging framework. -->
  <!-- Such propagation eliminates the performance impact of disabled log statements -->
  <!-- AT THE MOMENT THIS CAUSES AN java.lang.ClassCircularityError: java/util/logging/LogRecord -->
  <!-- <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator"> -->
  <!-- <resetJUL>true</resetJUL> -->
  <!-- </contextListener> -->

</configuration>
